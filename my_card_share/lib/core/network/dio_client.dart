import '../constants/api_constants.dart';

class DioClient {
  final String baseUrl;

  DioClient({this.baseUrl = ApiConstants.baseUrl});

  // Placeholder for Dio client configuration
  void init() {
    // Configured with interceptors, base options, timeouts
  }
}
