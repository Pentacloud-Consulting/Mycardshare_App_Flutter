import 'package:flutter/material.dart';

class EnterpriseInfoScreen extends StatelessWidget {
  const EnterpriseInfoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Enterprise Info')),
      body: const Center(child: Text('Enterprise Info')),
    );
  }
}
