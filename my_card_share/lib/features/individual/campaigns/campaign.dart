import 'package:flutter/material.dart';

class IndividualCampaignScreen extends StatelessWidget {
  const IndividualCampaignScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Campaign Tracking')),
      body: const Center(child: Text('Campaign Tracking')),
    );
  }
}
