import 'package:flutter/material.dart';

class MasterAdminWorkspaceScreen extends StatelessWidget {
  const MasterAdminWorkspaceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Platform Workspaces')),
      body: const Center(child: Text('Platform Workspaces')),
    );
  }
}
