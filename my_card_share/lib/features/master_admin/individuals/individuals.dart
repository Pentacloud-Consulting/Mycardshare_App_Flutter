import 'package:flutter/material.dart';

class MasterAdminIndividualsScreen extends StatelessWidget {
  const MasterAdminIndividualsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Individual Users Directory')),
      body: const Center(child: Text('Individual Users Directory')),
    );
  }
}
