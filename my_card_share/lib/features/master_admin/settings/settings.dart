import 'package:flutter/material.dart';

class MasterAdminSettingsScreen extends StatelessWidget {
  const MasterAdminSettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Platform System Settings')),
      body: const Center(child: Text('Platform System Settings')),
    );
  }
}
