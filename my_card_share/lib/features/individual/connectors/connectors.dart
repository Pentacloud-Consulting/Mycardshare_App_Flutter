import 'package:flutter/material.dart';

class IndividualConnectorsScreen extends StatelessWidget {
  const IndividualConnectorsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Integrations / Connectors')),
      body: const Center(child: Text('Integrations / Connectors')),
    );
  }
}
