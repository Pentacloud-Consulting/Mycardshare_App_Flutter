import 'package:flutter/material.dart';

class EnterpriseSettingsScreen extends StatelessWidget {
  const EnterpriseSettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Enterprise Settings & Billing')),
      body: const Center(child: Text('Enterprise Settings & Billing')),
    );
  }
}
