import 'package:flutter/material.dart';

class MasterAdminCompanyScreen extends StatelessWidget {
  const MasterAdminCompanyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Company Management')),
      body: const Center(child: Text('Company Management')),
    );
  }
}
