import 'package:flutter/material.dart';

class MasterAdminAnalyticsScreen extends StatelessWidget {
  const MasterAdminAnalyticsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Platform Usage Analytics')),
      body: const Center(child: Text('Platform Usage Analytics')),
    );
  }
}
