import 'package:flutter/material.dart';

class EnterpriseConnectorsScreen extends StatelessWidget {
  const EnterpriseConnectorsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Enterprise Connectors / CRM Sync')),
      body: const Center(child: Text('Enterprise Connectors / CRM Sync')),
    );
  }
}
