import 'package:flutter/material.dart';

class IndividualWorkforceScreen extends StatelessWidget {
  const IndividualWorkforceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Team Workforce')),
      body: const Center(child: Text('Team Workforce')),
    );
  }
}
