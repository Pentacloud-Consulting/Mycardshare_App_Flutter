import 'package:flutter/material.dart';

class EnterpriseAnalyticsScreen extends StatelessWidget {
  const EnterpriseAnalyticsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Enterprise Analytics & Reports')),
      body: const Center(child: Text('Enterprise Analytics & Reports')),
    );
  }
}
