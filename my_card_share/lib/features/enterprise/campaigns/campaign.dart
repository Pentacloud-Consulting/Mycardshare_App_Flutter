import 'package:flutter/material.dart';

class EnterpriseCampaignScreen extends StatelessWidget {
  const EnterpriseCampaignScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Corporate Campaigns')),
      body: const Center(child: Text('Corporate Campaigns')),
    );
  }
}
