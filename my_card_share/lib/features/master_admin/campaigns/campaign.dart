import 'package:flutter/material.dart';

class MasterAdminCampaignScreen extends StatelessWidget {
  const MasterAdminCampaignScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Global Campaigns')),
      body: const Center(child: Text('Global Campaigns')),
    );
  }
}
