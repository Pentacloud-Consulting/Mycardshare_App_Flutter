import 'package:flutter/material.dart';

class EnterpriseWorkspaceScreen extends StatelessWidget {
  const EnterpriseWorkspaceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Workspace & Employee Directory')),
      body: const Center(child: Text('Workspace & Employee Directory')),
    );
  }
}
